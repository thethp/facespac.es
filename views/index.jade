extend layout

block content
  #wrapper.home
    h1 facespaces

    form(method='post', action='/channel')
      p
        span http://facespac.es/c/
        input(type='text', name='channel', required)
        input(type='hidden', name='_csrf', value='#{csrf}')
      button(type='submit') Go
